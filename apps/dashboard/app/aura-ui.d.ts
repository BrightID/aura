// Base `a-*` intrinsic-element typings for React live in `@aura/ui/jsx/react`.
// Add dashboard-only custom elements/props here by declaring another `react`
// JSX.IntrinsicElements block — TS merges it with the shared one since both
// land in this program.
import '@aura/ui/jsx/react';

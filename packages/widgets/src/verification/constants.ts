// TODO: replace with the real unified domain before publishing. This widget
// can run cross-origin (embedded via the SDK on third-party sites), so a
// same-origin relative path won't resolve correctly here — it needs an
// absolute URL to the core app.
export const CORE_APP_URL = 'https://REPLACE-WITH-UNIFIED-DOMAIN.example/core'
